# Copy to `.env` (or use Vite mode files). Never commit real secrets.
# Base URL for REST API calls (no trailing slash).
VITE_API_URL=https://api.example.com

# Optional: set to "true" to hide assisted buying UI in the extension.
# VITE_DISABLE_ASSISTED_BUYING=false
