/** Hash-relative paths inside the federated remote (HashRouter). */
export const MODULE_ROOT = '/'

export const listPath = () => MODULE_ROOT

export const brandAddPath = () => '/add'

export const brandDetailPath = (brandId: string) => `/${brandId}`
