import {
  BackButton as LibraryBackButton,
  type BackButtonProps,
} from '@emporix/component-library'

const BackButton = (props: BackButtonProps) => {
  return <LibraryBackButton {...props} />
}

export default BackButton
